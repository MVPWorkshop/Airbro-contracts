import { expect } from "chai";

export function shouldDeploy(): void {
  // airbroFactory does not have Ownable import, so not sure what test to write for a basic check of the contract deployment
  // it("should have contract owner to address of deployer upon deployment", async function () {
  //   expect(await this.airbroFactory.owner()).to.equal(this.signers.deployer.address);
  // });
}
